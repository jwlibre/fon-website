{
    "name": "fields_website",
    "version": "1.0.0",
    "description": "",
    "source": "src/index.html",
    "private": true,
    "scripts": {
        "start": "rm -rf dist/* && parcel",
        "build": "rm -rf dist/* && parcel build"
    },
    "dependencies": {
        "three": "^0.136.0"
    },
    "devDependencies": {
        "parcel": "^2.0.1"
    }
}
