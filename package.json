{
    "name": "fields_website",
    "version": "1.0.0",
    "description": "",
    "source": "src/index.html",
    "private": true,
    "scripts": {
        "start": "find dist -maxdepth 1 -type f -delete && parcel",
        "build": "find dist -maxdepth 1 -type f -delete && parcel build"
    },
    "dependencies": {
        "gsap": "^3.9.1",
        "three": "^0.136.0"
    },
    "devDependencies": {
        "parcel": "^2.0.1"
    }
}
